node_modules
dist
coverage
.turbo
.next
.DS_Store
*.log
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example
*.db
*.db-journal
*.sqlite
*.sqlite-journal
*.tsbuildinfo

# next-pwa generated service worker artifacts (regenerated on every web build)
apps/web/public/sw.js
apps/web/public/sw.js.map
apps/web/public/workbox-*.js
apps/web/public/workbox-*.js.map

# Local DB dumps taken before destructive migrations — never commit (real data)
backups/

# Daily portfolio review — headless Claude Code job driven by a systemd timer.
# Deliberately untracked: it is server-local ops wiring, not app code, and it runs
# from absolute paths in the working tree (git pull never touches untracked files).
claude-cron/
docs/features/portfolio-daily-review/
